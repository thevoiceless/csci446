<!DOCTYPE html>

<html>
<head>
	<title>Rolling Stone's Top 100 Albums of All Time</title>
	<link rel="stylesheet" type="text/css" href="list.css">
</head>
<body>
	<h1>Rolling Stone's Top 100 Albums of All Time</h1>
	<p>Sorted by <%= request.GET()["order"] %></p>
	<table>
		<% @db.execute("select * from albums order by " + request.GET()["order"]) do |row| %>
			<% if row['rank'] == rank_to_highlight %>
				<tr id="highlight">
			<% else %>
				<tr>
			<% end %>
				<td><%= row['rank'] %></td>
				<td><%= row['title'] %></td>
				<td><%= row['year'] %></td>
			</tr>
		<% end %>
	</table>
</body>
</html>